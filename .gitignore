# SDD scratch workspace (ledger, briefs, review packages)
.superpowers/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/

# Test hardening scratch: hypothesis' failing-example database and the
# cosmic-ray session databases. Both are per-machine caches, not results -
# the results that matter are written up in docs/test-hardening.md.
.hypothesis/
*.sqlite

# Coverage data. The gate runs in CI and in every M4 task checkpoint, so this
# file reappears constantly and must never be committed.
.coverage
.coverage.*
htmlcov/
